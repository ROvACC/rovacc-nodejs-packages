import { trainingEventsTypes } from './training-events-types';

describe('trainingEventsTypes', () => {
  it('should work', () => {
    expect(trainingEventsTypes()).toEqual('training-events-types');
  });
});
